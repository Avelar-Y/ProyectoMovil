rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Minimal rules: allow read to authenticated users and deny writes (adjust in production)
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
